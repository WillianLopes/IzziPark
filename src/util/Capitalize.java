package util;


/*public class Capitalize {  
    
String silva="";  
String posicao="";  
String mais="";  
String tudo="";  
String maiuscula2="";  

	public String Capitalize(String s) {  
		posicao = "" + s.charAt(0);//pega a primeira letra que sera maiuscula  
		String pos = posicao.toUpperCase();    //transforma em maiuscula  
		  
		int tamanho = s.length();//ve o tamanho da frase  
		for(int i = 1; i < s.length(); i++) {  //coloca o for de acordo com o tamanho  
			mais = mais + s.charAt(i); //acrescenta as letras  
			   
			if( s.charAt(i) == ' ') {  //se houver um espaço a próxima vai ser transformada em maisucula.  
				String maiuscula = "" + s.charAt(i+1);//pega a próxima apos o espaço    
				maiuscula2 = maiuscula.toUpperCase();       //transforma em maiuscula.  
				mais = mais + maiuscula2;//acrescenta a maiuscula a palavra completa, que se chama mais  
				i = i + 1; //soma um, pois uma letra minuscula foi substituida e ja foi acrescentada                                   
			} 
		} 
		    
		tudo = pos + mais;   
		   
		System.out.println("tudo:"+tudo);              
	}      
      
      
}  */